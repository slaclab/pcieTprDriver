 Release Notes for the pcieTprDriver package
---------------------------------------------

R0.6.1   Kukhee Kim (Sep 23, 2024)
         - align with recent kernel driver updates in the evr-card-g2 project

R0.6.0   Marcio Donadio (Jul 21, 2023)
         - From Matt Weaver: Fix bug in kernel driver which hung on corrupt DMA
           message.

R0.5.1   Bruce Hill (Dec 05, 2022)
         Rework src/load_module.sh to work in different shells.
         Add support for building tpr.ko for each kernel module src installed on host.
         Allow buildroot to build and install from TOP level make.

R0.5.0   Kukhee Kim (Aug 31, 2022)
         - add CPU timestamp count into the timing queue entry
         - pick-up changes from the GitHub and merge it

R0.4.0   Kukhee Kim (Aug 29, 2022)
         - bug fixing for handling/tracking open/close device fils

R0.3.1   Kukhee Kim (March 10, 2022)
         - add firmware image 968bb5f

R0.3.0   Matt Weaver (Dec 16, 2021)
         - fix bug the device file opens fail after 256 previous opens

R0.2.0   Kukhee Kim (Sep 24, 2021)
         - bug fix related to interrupt and dma handling
           . adding volatile for hardware registers
         - compatible firmware version
           EvrCardG2-CED2001D-20210916183225-weaver-968bb5f.mcs

R0.1.0   Kukhee Kim (Sep 9, 2021)
         - initial release
         - get kernel driver from evr-card-g2/maser
         - compatible firmware version
           EvrCardG2-CED2001B-20210726120051-weaver-d381d3e.mcs
